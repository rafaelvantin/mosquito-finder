import { View, Text } from 'react-native';

const ViewReports = () => {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>ViewReports</Text>
        </View>
    );
}

export default ViewReports;